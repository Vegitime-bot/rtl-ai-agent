# ============================================================
# RTL AI Agent — Python 3.11 의존성
# ============================================================
# 사내망(오프라인) 설치 순서:
#   1. 아래 버전으로 wheel 파일 미리 다운로드 (인터넷 환경에서):
#      pip download -r requirements.txt \
#          --python-version 311 --platform manylinux2014_x86_64 \
#          -d offline_wheels/
#   2. 사내망 서버에서:
#      pip install --no-index --find-links offline_wheels/ -r requirements.txt
# ============================================================

# ── 코어 ────────────────────────────────────────────────────
requests==2.31.0
PyYAML==6.0.1
pygls==2.0.1
neo4j==5.19.0

# ── Vector RAG ─────────────────────────────────────────────
# Python 3.11 + 사내망 오프라인 설치 호환 버전 고정
#
# 의존성 체인 (충돌 방지용 고정):
#   FlagEmbedding 1.2.11
#     └─ transformers 4.40.2
#         └─ tokenizers  0.19.1   ← Rust 바이너리; cp311 wheel 필수
#             └─ huggingface_hub 0.23.4
#   torch 2.2.2 (CPU)             ← faiss-cpu 와 numpy ABI 일치
#   numpy 1.26.4                  ← numpy 2.x 는 torch 2.2 와 충돌
#
numpy==1.26.4
torch==2.2.2          --index-url https://download.pytorch.org/whl/cpu
transformers==4.40.2
tokenizers==0.19.1
huggingface_hub==0.23.4
safetensors==0.4.3
FlagEmbedding==1.2.11
faiss-cpu==1.7.4
