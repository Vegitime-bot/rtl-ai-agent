# Kimi k2.5 설정
provider: openai          # OpenAI 호환 API
endpoint: https://api.moonshot.cn/v1  # ← 실제 엔드포인트로 교체
model: moonshot-v1-256k   # ← 실제 모델명으로 교체

max_tokens: 16384   # spec/plan/summary 공용 + codegen용 (codegen은 별도 output_max_tokens로 override 가능)
# patch mode 전용 출력 토큰 상한 (미설정 시 원본 크기 기반 자동 계산)
# patch_header_max_tokens: 2048   # header(port list) 재작성 출력 토큰
# patch_decl_max_tokens: 512      # 신호 선언 추가 출력 토큰

context_window: 256000
temperature: 0.2
timeout: 7200
req_interval: 1.0
rate_limit_retries: 5
rate_limit_retry_base: 60
llm_log: "logs/llm.jsonl"
verify_causal_threshold: 0.5
# api_key: ""
