# LLM endpoint settings (provider: openai | claude)
provider: claude
endpoint: https://api.anthropic.com/v1
model: claude-sonnet-4-6
max_tokens: 4096
api_key: ""  # set MODEL_API_KEY env var instead of committing secrets
